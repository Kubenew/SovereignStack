# =============================================================================
# SovereignStack — All Dependencies (convenience wrapper)
# For tiered installation, see the requirements/ directory:
#   requirements/core.txt          — minimum for core + conformance
#   requirements/experimental.txt  — core + GraphQL, OpenAI, SPIFFE
#   requirements/conformance.txt   — minimal conformance runner only
#   requirements/dev.txt           — full development environment
#
# Update pinned versions via:
#   pip-compile --generate-hashes --output-file=requirements-locked.txt requirements.txt
# =============================================================================
-r requirements/experimental.txt
